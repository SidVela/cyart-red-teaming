# Elastic Stack Installation Notes

## Objective
Prepare Kali Linux environment for Elastic Stack threat hunting setup.

## Steps Completed

1. Updated Kali Linux packages.
2. Installed required dependencies.
3. Verified Java installation.

## Commands Used

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install curl wget apt-transport-https gnupg unzip -y
java -version
```

## Result

Java environment prepared successfully for Elastic Stack installation.
